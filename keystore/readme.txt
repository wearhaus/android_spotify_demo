


password is: shareandenjoy

for both keystore and alias.  This is because we dont intend to actually release this onto PlayStore as is.  So this is more of a debug for others to install via distributable apk



